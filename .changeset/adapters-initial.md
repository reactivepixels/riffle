---
'@rpxl/riffle': minor
---

Add keyboard navigation, `getLabel` accessible names, `'auto'` card sizing, a complete
`update()` for every option, and `createAdapterHandle` for building framework adapters,
plus the React and Vue entries themselves: `@rpxl/riffle/react`'s `useRiffle`,
`useRiffleState`, and `<Riffle>`; `@rpxl/riffle/vue`'s `useRiffle`, the `v-riffle-root`
and `v-riffle-card` directives, and `<Riffle>`.
