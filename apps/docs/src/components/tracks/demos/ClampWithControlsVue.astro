---
// The Vue clamp-with-controls demo: examples/vue-clamp-controls' own App,
// the same file the Clamp with controls recipe's snippets come from. Its
// own <style scoped> reserves its fan room, so only the dark stage (see
// .recipe-demo-stage in custom.css, for contrast against Starlight's own
// light theme) is added here. See src/lib/track-demos.ts for why each
// island has a wrapper like this one.
import App from '../../../../../../examples/vue-clamp-controls/src/App.vue'
---

<div class="recipe-demo-stage not-content">
  <App client:load />
</div>
