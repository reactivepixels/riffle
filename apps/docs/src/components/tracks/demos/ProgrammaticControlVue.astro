---
// The Vue programmatic-control demo: examples/vue-recipes' own
// ExternalControlStack.vue, the same file the Programmatic control
// recipe's snippets come from. It assumes the same `.stage--fan` room
// vue-recipes' own App.vue shell reserves for it, and that app's own dark
// page, so this wrapper gives it both: see .recipe-demo-stage and
// .recipe-demo-stage--fan in custom.css. See src/lib/track-demos.ts for why
// each island has a wrapper like this one.
import ExternalControlStack from '../../../../../../examples/vue-recipes/src/ExternalControlStack.vue'
---

<div class="recipe-demo-stage recipe-demo-stage--fan not-content">
  <ExternalControlStack client:load />
</div>
