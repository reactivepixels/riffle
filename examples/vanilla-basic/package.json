{
  "name": "vanilla-basic",
  "version": "0.0.0",
  "private": true,
  "description": "Riffle with no framework and no build step: a single HTML file.",
  "scripts": {
    "dev": "pnpm dlx serve ."
  }
}
