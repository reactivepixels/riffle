{
  "name": "vanilla-recipes",
  "version": "0.0.0",
  "private": true,
  "description": "Five vanilla Riffle recipes: a vertical fan, clamped bounds with disabled controls, an infinite feed, programmatic control, and forms inside cards.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@rpxl/riffle": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^2.1.9"
  }
}
